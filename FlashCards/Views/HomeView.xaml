<UserControl x:Class="FlashCards.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:FlashCards.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Home Page" Style="{StaticResource MaterialDesignHeadline1TextBlock}"/>
            <TextBlock Text="Vælg et bibliotek" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
            <ComboBox Style="{StaticResource MaterialDesignComboBox}" ItemsSource="{Binding FlashCards}" SelectedIndex="0" DisplayMemberPath="LibName" HorizontalAlignment="Left" Width="250"/>
        </StackPanel>
    </Grid>
</UserControl>
